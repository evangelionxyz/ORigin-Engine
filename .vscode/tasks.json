{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build_sandbox",
            "command": "make",
            "args": [
                "-j",
                "3",
                "Sandbox"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}